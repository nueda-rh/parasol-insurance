---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: models-cache
  namespace: ic-shared-llm-ds
  labels:
    app: ic-shared-llm-ds
  annotations:
    argocd.argoproj.io/sync-wave: "0"
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 100Gi
  storageClassName: ocs-storagecluster-cephfs
  volumeMode: Filesystem